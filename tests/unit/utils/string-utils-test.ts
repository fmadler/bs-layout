import bsUtilsStringUtils from 'dummy/utils/string-utils';
import { module, test } from 'qunit';

module('Unit | Utility | bs/utils/string-utils', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = bsUtilsStringUtils();
    assert.ok(result);
  });
});
